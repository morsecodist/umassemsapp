# Umass EMS Website

## Building

To build (with watch):

`gulp`

To build (without watch):

`gulp build`

To watch:

`gulp watch`

## Development Server

Build, run server, and watch:

`npm start`

To run just run development server:

`node server`
